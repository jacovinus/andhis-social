import { TextPreviewPipe } from './text-preview.pipe';

describe('TextPreviewPipe', () => {
  it('create an instance', () => {
    const pipe = new TextPreviewPipe();
    expect(pipe).toBeTruthy();
  });
});
